(defwidget widget.player []
  (button 
    :class "widget-player"
    :onclick "playerctl play-pause" 
    (label 
      :limit-width 15
      :text { 
        (is-playing == "Playing" ? `` : "" ) + " ${!matches(music-title," *") ? music-title : '-'}"
      }))
  )
