(defwidget widget.audio []
  (button 
    :class "widget-audio"
    :onclick "eww open --toggle audio" 
    {" "+ (
            is-muted == "yes" 
            ? "Muted" 
            : volume+"%"
            )})
  )
